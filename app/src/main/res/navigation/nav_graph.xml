<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph.xml"
    app:startDestination="@id/stationsPickerFragment">

    <fragment
        android:id="@+id/trainCountFragment"
        android:name="com.ecs.hellobart.TrainCountFragment"
        android:label="train_count_fragment"
        tools:layout="@layout/train_count_fragment" >
        <action
            android:id="@+id/action_trainCountFragment_to_stationsPickerFragment"
            app:destination="@id/stationsPickerFragment" />
    </fragment>

    <fragment
        android:id="@+id/stationsPickerFragment"
        android:name="com.ecs.hellobart.StationsPickerFragment"
        android:label="stations_picker_fragment"
        tools:layout="@layout/stations_picker_fragment" />
</navigation>